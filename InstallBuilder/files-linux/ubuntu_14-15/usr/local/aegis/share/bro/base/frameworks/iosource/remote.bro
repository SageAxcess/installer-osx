module PcapRemote;

export {
	const snaplen = 1000 &redef;
	const bufsize = 8192 &redef;
	const key_file = "" &redef;
	const interface = "eth0" &redef;
	const remote_host = "" &redef;
	const remote_user = "" &redef;
	const executable = "tcpdump" &redef;
}

